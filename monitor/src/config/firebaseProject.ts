/**
 * Default Firebase / GCP project for the TECHNOVA app (see monitor/.firebaserc).
 * The project ID string is fixed at creation; only the display name in
 * Firebase Console can be relabelled to “Technova / TECHNOVA”.
 */
export const FIREBASE_PROJECT_ID = "roudi-monitor-app" as const
